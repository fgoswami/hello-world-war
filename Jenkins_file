pipeline {
        agent any
        stages ('checkout') {
        steps {
        sh "git clone https://github.com/fgoswami/hello-world-war.git"
        }
        }
        stage ('build')
    {
    sh "mvn clean package"
    }
    }
    }
