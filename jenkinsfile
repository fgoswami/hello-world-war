pipeline {
    agent { label 'java' }
    stages {
        stage('checkout') { 
            steps {
              sh "git clone "
            }
        }
stage('build') { 
            steps {
              sh "mvn clean package"
            }
        }        
    }
}
